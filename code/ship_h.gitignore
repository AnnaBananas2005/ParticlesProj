#pragma once
//Space ship pack:
//https://foozlecc.itch.io/void-main-ship
//Void (our main ship)

#include <SFML/Graphics.hpp>
using namespace sf;

class Ship {
private:
	Vector2f m_Position;
	Sprite m_Sprite;
	Texture m_Texture;
	Vector2f m_Resolution;
	IntRect m_Arena;

public:
	Ship();
	void spawn(IntRect spaceArena, Vector2f resolution);
	FloatRect getPosition();
	Vector2f getCenter();
	float getRotation();
	Sprite getSprite();
	void update(float elapsedTime, Vector2i mousePosition);
};
